/* Export your Google Sheet to dist/offers.csv, then set sheetCsvUrl to "./offers.csv".
   An empty URL keeps the clearly marked example offers. */
window.BN_CONFIG = {
  sheetCsvUrl: "",
  refreshMinutes: 15
};
